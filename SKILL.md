---
name: knowledge-cards
description: 用「原子卡片 + 双链 + Obsidian canvas」管理学习型知识库的完整方法论：卡片模板与写作原则、按知识板块的文件夹组织、frontmatter tags/aliases 约定、canvas 导航图（含力导向自动布局脚本）、自动生成的导航文件与断链报告，以及把讲义/教材/论文整理成卡片的批量工作流。只要用户提到 建知识库 / 知识卡片 / 卡片笔记 / 整理知识点 / 添加词条 / 维护 canvas 画布 / 知识库重组 / 把讲义整理成卡片 / 读懂这篇论文需要哪些知识点，甚至只是展示出一个由扁平 markdown 卡片组成的学习笔记目录，都应使用本 skill。
---

# Knowledge Cards —— 卡片式知识库管理

为「学习型知识库」（典型场景：数学、物理等理论课程）提供一套经过实践校准的管理体系。核心信念：**双链是承重墙**（零维护成本、自索引），文件夹和标签只做轻量分区，canvas 做导航，索引靠脚本生成不靠手写。

## 核心理念（写卡前必读）

1. **原子性**：一张卡一个概念/定理。判断标准：复习时会不会单独问"这是什么"。
2. **固定模板，栏目顺序稳定**（模板见 `references/card-templates.md`）：概念卡的灵魂是**定义拆解 + 反例**；定理卡的灵魂是**证明骨架 + 客户名单（用在哪）**。
3. **双链是灵魂**：关联栏必须用 `[[链接]]`；正文首次提及概念时也链接。链接按文件名解析，移动文件不断链。
4. **反例与正例同等重要**：每张定义卡都问"最小的反例是什么"。病态样本（反例）可以反复出现在多张卡里，但每张卡只取它的一个侧面。
5. **用自己的话重述**——如果用户委托生成卡片，产出的卡片应交给用户改写；改写这一步是卡片生效的核心，不要省。
6. **拆分而非堆砌**：写卡过程中发现缺失的依赖概念时，拆成独立的新卡，不要塞进当前卡的正文。复合内容的拆分判据见 `references/card-templates.md` 写作细则。
7. **知识卡只装知识**：知识库的定位是「读懂论文/教材所需知识点的查考库」——卡片只记知识点本身，"我的研究/项目用它做什么"不进卡（要串联就另画研究画布，不污染卡片）。定理卡的"用在哪"只写领域内公认的客观下游。

## 知识库结构约定

```
<知识库根目录>/
├── 导航.md            ← 由 scripts/生成导航.sh 生成，勿手写
├── 生成导航.sh        ← 从 skill 的 scripts/ 复制而来
├── 布局画布.sh        ← 同上
├── 知识总览.canvas    ← 全库总图（可手工维护）
├── 01-<板块名>/       ← 知识板块（数字前缀 = 学习顺序）
│   ├── 概念A.md
│   └── ...
├── 02-<板块名>/
└── 02-<板块名>.canvas ← 每个板块一张子画布
```

维度分工（不可混用）：

| 维度 | 载体 | 说明 |
| --- | --- | --- |
| 知识板块 | 文件夹 | 唯一的目录维度；客观、静态、归属唯一 |
| 卡片类型 | frontmatter `tags` | **封闭词表：仅 `[概念]` / `[定理]`**，不要发明新标签 |
| 别名/多名字 | frontmatter `aliases` | 中英、新旧术语、常见变体（如 漫没/淹没） |
| 依赖/导航 | canvas + 双链 | 表达力最强的层次，不占目录维度 |
| 掌握程度 | frontmatter 属性 | 用户自查自填，或留给 Dataview |
| 来源索引 | 卡内文字引用 | **不要用"定理 2.3.4"式编号**（用户可能换教材），用语义说法或卡名 |

## 卡片模板概要

**概念卡**：YAML frontmatter（tags + aliases）→ `# 标题` → `> [!abstract] 一句话` → `## 定义` → `## 直觉/拆解`（定义的每个条件各挡住什么病态）→ 可选栏目（`等价刻画`/`性质`/`判据·求法`，封闭词表、位置固定、有料才写）→ `## 例子`（表格速查）→ `## 反例 / 易错点` → `## 关联`（双链列表）。核心栏目之外的信息走可选栏目，不丢也不破坏一致性。

**定理卡**：`## 陈述`（精确，分条款）→ `## 直觉` → `## 证明思路`（只记骨架：哪一步需要真正构造、哪些是标准搬运）→ `## 用在哪`（下游客户名单）→ `## 反例 / 边界`（去掉哪个条件会失效）→ `## 关联`。

完整模板、范例与写作细则见 `references/card-templates.md`；canvas JSON 格式、颜色约定与布局脚本参数见 `references/canvas-conventions.md`。写卡或改画布前先读对应文件。

## 工作流

### A. 初始化新知识库

```bash
bash <skill目录>/scripts/init_kb.sh <目标目录> <板块1> <板块2> ...
```

脚本会建目录、把 `生成导航.sh`、`布局画布.sh` 复制进库根。之后告诉用户：日常维护只有两个动作——新卡放进板块文件夹、随时跑 `./生成导航.sh`。

### B. 添加卡片（单张或一组）

1. 判断归属板块与类型（概念/定理），确定文件名（定理直接用定理名，如 `隐函数定理.md`）；
2. 按模板写卡（frontmatter tags + aliases 必须有；正文双链；**无定理编号、无元话语**如"建议在画布上…"）；
3. 在对应板块 canvas 加节点与连线（JSON 格式与颜色见 references）；需要时跑 `./布局画布.sh <板块>.canvas` 重排；
4. 跑 `./生成导航.sh` 刷新索引。

### C. 把讲义/教材/论文整理成卡片（批量）

1. **提取**：用 pdftotext 提取文本（`pdftotext -layout <pdf> tmp/`；大文件先估算 token 再决定是否分段）；
2. **读原文**，列出"读懂本章/本文所需的知识点"清单（概念/定理分类）——只收读懂所需；论文自身的论证主线、与读者研究的关联不进库；
3. **对齐术语**：若库里已有同主题卡片，先把现有卡片与原文的符号、说法统一（这一步在写新卡**之前**做）；
4. 按模板成批写卡，缺的依赖概念拆独立卡；**来源规则**：公认知识（任何教材查到都一样）按标准写法、不标来源；论文特有的定义、记号、结论在卡内标注论文出处；
5. 更新板块 canvas（新节点 + 连线），跑布局与导航脚本；
6. 校验：canvas JSON 合法、无悬空边、无节点重叠、wiki 链接无断链、tags 齐全（一段 python 全查完）。

### D. 维护与重组

- **移动/改名卡片**：wiki 链接自动跟随；但 canvas 的 `file` 字段是按路径的，必须同步更新（脚本处理或手工改 JSON）；改完重跑导航脚本。
- **新板块出现**（讲义推进到新主题）：新建 `NN-<板块名>/`，照抄子画布模式；单板块超 40 张再拆子目录。
- **布局审计**：板块 canvas 用 `--分层` 模式跑一次，若某张卡摆位与直觉冲突，多半是它的归属或依赖记录有错。

## 脚本

| 脚本 | 用法 | 作用 |
| --- | --- | --- |
| `scripts/init_kb.sh` | `init_kb.sh <目录> <板块...>` | 初始化库结构并安装脚本 |
| `scripts/生成导航.sh` | 库根目录下 `./生成导航.sh` | 生成 `导航.md`：分板块卡片清单（双链+tags+一句话+aliases）+ **断链报告**（断链 = 用户给自己留的坑，当作待办清单交付） |
| `scripts/布局画布.sh` | `./布局画布.sh [画布...]`（默认力导向；`--分层` 切换） | canvas 自动布局：节点互斥+弹簧+对齐力（箭头刚性），挂边按"直线穿盒"法；分层模式适合纯 DAG 依赖图 |

布局参数调优：整体松紧改 `k`（理想边长，默认 330）；横平竖直感改对齐力系数（默认 0.25）。

## 沟通要点

- 用户委托批量生成卡片时，**先给 1-2 张样板等确认**再批量（本方法论的形成过程就是用户不断修剪样板的过程）；
- 交付时用 `present` 呈现新卡与更新的画布/导航；
- 主动汇报校验结果（断链、重叠、tags 统计）——这是这套体系的"免费质检"。
